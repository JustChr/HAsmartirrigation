# The built panel bundle is committed (HACS installs the tag's source tree).
# Pin it to LF so the CI freshness check (rebuild + git diff) is never tripped
# by line-ending conversion between platforms.
custom_components/irrigation_plus/frontend/dist/irrigation-plus.js text eol=lf
